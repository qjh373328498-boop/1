# 🎬 一体化视频生成工作流（完整版）

## 完整流程概览

```
参考资料 → 创意 → 脚本 → 配音 → 分镜 → 素材 → 视频 → 字幕 → BGM → 多语言 → 成品
```

---

## 技能矩阵（23 技能整合版）

| 阶段 | 技能 | 工具 | 输出 | 批次 |
|------|------|------|------|------|
| **0. 资料收集** | YouTube 下载 | baoyu-youtube-transcript | 参考视频 + 文稿 | 第 1 批 |
| | 网页提取 | baoyu-url-to-markdown | 参考资料.md | 第 1 批 |
| **1. 创意** | 头脑风暴 | brainstorming | 创意点子.md | 第 3 批 |
| **2. 脚本** | 文案生成 | Claude | script.txt | - |
| **3. 配音** | TTS 语音 | tts-voice | narration.mp3 + vtt | - |
| **4. 分镜** | 场景分析 | Claude | storyboard.json | - |
| | 漫画分镜 | baoyu-comic | comic_*.png | 整合 |
| **5. 素材** | 通用图片 | baoyu-image-gen | frame_*.png | - |
| | 高质量图片 | baoyu-imagine | frame_*.png | 整合 |
| | 数据图表 | baoyu-infographic | chart.png | - |
| | 封面图 | baoyu-cover-image | cover.png | - |
| | 流程图 | baoyu-diagram | diagram.png | 第 1 批 |
| | 旁白配图 | baoyu-article-illustrator | illustration_*.png | 第 3 批 |
| | 信息卡片 | baoyu-image-cards | card_*.png | 第 3 批 |
| | 小红书配图 | baoyu-xhs-images | xhs_*.png | - |
| **6. 视频** | 图片转视频 | FFmpeg | visuals.mp4 | - |
| | HTML 动画 | hyperframes | animation.mp4 | - |
| **7. 字幕** | 字幕同步 | auto-subtitles | subtitles.srt | - |
| | 多语言翻译 | baoyu-translate | subtitles_en.srt | 第 1 批 |
| **8. BGM** | 背景音乐 | music-gen | bgm.mp3 | - |
| **9. 剪辑** | 最终合成 | video-use | final_video.mp4 | - |
| **10. 发布** | 格式优化 | baoyu-format-markdown | 说明文档.md | 第 1 批 |
| | 图片压缩 | baoyu-compress-image | compressed_*.png | 第 2 批 |
| | 封面发布 | baoyu-post-to-wechat | 公众号文章 | 第 2 批 |
| | 微博发布 | baoyu-post-to-weibo | 微博推文 | 第 2 批 |
| | Twitter 发布 | baoyu-post-to-x | X 推文 | 第 2 批 |

---

## 触发方式

### 方式 1：一体化生成（推荐）
```
用户："帮我做一个产品介绍视频"
触发：video-production-pipeline（总控）
```

### 方式 2：分步执行
```
用户："先下载几个参考视频" → baoyu-youtube-transcript
用户："帮我写脚本" → 文案生成
用户："把脚本转成语音" → tts-voice
用户："根据脚本生成图片" → baoyu-image-gen
用户："合成视频" → video-use
```

### 方式 3：资料增强模式
```
用户："找几个 YouTube 参考视频" → baoyu-youtube-transcript
用户："提取这个网页的内容" → baoyu-url-to-markdown
用户："基于这些资料写脚本" → 文案生成
```

---

## 完整工作流程

### 阶段 0：资料收集（新增 - 第 1 批）

#### 0.1 YouTube 视频下载
```bash
# 下载参考视频并提取字幕
baoyu-youtube-transcript \
  --url "https://youtube.com/watch?v=xxx" \
  --output-dir references/ \
  --extract-transcript

# 输出：
# - references/video.mp4（视频文件）
# - references/transcript.md（文稿）
# - references/subtitles.srt（字幕）
```

**用途**：
- 竞品分析
- 学习优秀视频结构
- 提取参考文稿

#### 0.2 网页内容提取
```bash
# 提取网页为 Markdown
baoyu-url-to-markdown \
  --url "https://example.com/article" \
  --output references/article.md

# 批量提取
baoyu-url-to-markdown \
  --urls references/urls.txt \
  --output-dir references/
```

**用途**：
- 收集参考资料
- 提取产品信息
- 整理数据素材

---

### 阶段 1：创意构思（第 3 批）

#### 1.1 头脑风暴
```bash
# 调用 brainstorming skill
# 输出：ideas.md

头脑风暴维度：
- 视频主题
- 目标受众
- 内容调性（专业/轻松/幽默）
- 呈现形式（图文/口播/动画）
- 差异化亮点
```

---

### 阶段 2：脚本生成

```bash
# 基于资料和创意生成脚本
# 输出：script.txt

脚本结构：
- 视频标题
- 开场白（3-5 秒，吸引注意）
- 主体内容（30-120 秒，核心信息）
- 行动号召（5 秒，引导互动）
```

---

### 阶段 3：配音生成（tts-voice）

```bash
# 使用 tts-voice skill
python3 -m edge_tts \
  --voice zh-CN-YunxiNeural \
  --file script.txt \
  --write-media narration.mp3 \
  --write-subtitles narration.vtt
```

**输出**：
- `narration.mp3` - 人声音频
- `narration.vtt` - 字幕文件

---

### 阶段 4：分镜脚本生成

```json
// 输出：storyboard.json
{
  "frames": [
    {
      "id": 1,
      "time": "00:00-00:05",
      "narration": "大家好，今天介绍...",
      "image_prompt": "产品封面图，科技感",
      "image_type": "baoyu-cover-image",
      "image_file": "frame_01.png"
    },
    {
      "id": 2,
      "time": "00:05-00:15",
      "narration": "这个产品有三大特点...",
      "image_prompt": "三点信息图表",
      "image_type": "baoyu-infographic",
      "image_file": "frame_02.png"
    }
  ]
}
```

---

### 阶段 5：素材生成（核心优化）

#### 5.1 通用图片生成
```bash
# 遍历分镜，生成每帧图片
for frame in storyboard.frames:
  baoyu-image-gen \
    --prompt "{frame.image_prompt}" \
    --image "{frame.image_file}" \
    --ar "16:9" \
    --quality "high"
```

#### 5.2 高质量图片（整合）
```bash
# 使用 baoyu-imagine 生成更高质量封面/插图
baoyu-imagine \
  --prompt "高质量封面图，科技感，细节丰富" \
  --image "cover_hq.png" \
  --ar "16:9" \
  --quality "2k"

# 批量生成（适用于已完成分镜脚本的场景）
baoyu-imagine \
  --batchfile prompts/batch.json \
  --jobs 6
```

#### 5.2 数据图表（第 1 批）
```bash
# 生成数据可视化图表
baoyu-infographic \
  --title "用户增长率" \
  --data "Q1:20%, Q2:35%, Q3:50%, Q4:80%" \
  --style "bar_chart" \
  --image "growth_chart.png"
```

#### 5.3 流程图（第 1 批）
```bash
# 生成流程图/示意图
baoyu-diagram \
  --type "flowchart" \
  --nodes "开始→分析→处理→输出→结束" \
  --style "modern" \
  --image "workflow.png"
```

#### 5.4 封面图
```bash
baoyu-cover-image \
  --title "产品评测" \
  --subtitle "深度体验 30 天" \
  --style "tech" \
  --image "video_cover.png"
```

#### 5.5 旁白配图（第 3 批）
```bash
# 根据旁白内容自动配图
baoyu-article-illustrator \
  --text "旁白文本" \
  --style "illustration" \
  --image "illustration_01.png"
```

#### 5.6 信息卡片（第 3 批）
```bash
# 生成关键信息卡片
baoyu-image-cards \
  --text "核心观点/金句/数据" \
  --style "minimal" \
  --image "quote_card.png"
```

#### 5.7 漫画分镜（整合）
```bash
# 使用 baoyu-comic 生成故事性视频分镜/漫画式叙事
# 适用于：故事性视频、教程漫画、知识科普视频

# 基础用法 - 简单分镜
baoyu-comic \
  --storyboard "场景 1: 开场→场景 2: 问题→场景 3: 解决方案" \
  --art "manga" \
  --tone "warm" \
  --layout "standard" \
  --output-dir "comic-storyboard/"

# 完整工作流 - 知识漫画
baoyu-comic \
  --source "script.md" \
  --art "ligne-claire" \
  --tone "neutral" \
  --layout "cinematic" \
  --prompts-only  # 仅生成提示文件，便于审查

# 批量生成页面（基于已生成的提示文件）
baoyu-comic \
  --images-only \
  --comic-dir "comic-storyboard/"
```

#### 5.8 小红书配图
```bash
# 生成小红书风格配图
baoyu-xhs-images \
  --title "笔记标题" \
  --style "cute" \
  --ratio "3:4" \
  --image "xhs_cover.png"
```

---

### 阶段 6：视频生成

#### 6.1 图片转视频
```bash
# 将图片序列转为视频
ffmpeg -framerate 1/3 -i frame_%02d.png \
  -c:v libx264 -pix_fmt yuv420p \
  -vf "scale=1920:1080" \
  visuals.mp4
```

#### 6.2 HTML 动画（可选）
```bash
# 使用 hyperframes 生成动画视频
npx hyperframes render video.html -o animation.mp4
```

---

### 阶段 7：字幕处理

#### 7.1 字幕同步
```bash
# 使用 VTT 转为 SRT
ffmpeg -i narration.vtt subtitles.srt
```

#### 7.2 多语言翻译（第 1 批）
```bash
# 翻译字幕为多语言
baoyu-translate \
  --input subtitles.srt \
  --source-lang zh \
  --target-lang en \
  --output subtitles_en.srt

baoyu-translate \
  --input subtitles.srt \
  --source-lang zh \
  --target-lang jp \
  --output subtitles_jp.srt
```

---

### 阶段 8：背景音乐生成

```bash
# 生成 BGM
python music_gen.py \
  --style "light corporate" \
  --duration 60 \
  --output bgm.mp3
```

---

### 阶段 9：最终合成（video-use）

```bash
# 合并所有轨道
ffmpeg -i visuals.mp4 \
  -i narration.mp3 \
  -i bgm.mp3 \
  -i subtitles.srt \
  -filter_complex "[1][2]amix=inputs=2:duration=longest,volume=volume=0.8" \
  -vf "subtitles=subtitles.srt" \
  -c:v libx264 -c:a aac \
  final_video.mp4
```

---

### 阶段 10：发布准备（第 2 批）

#### 10.1 格式优化（第 1 批）
```bash
# 格式化说明文档
baoyu-format-markdown \
  --input README_raw.md \
  --style "github" \
  --output README.md
```

#### 10.2 图片压缩（第 2 批）
```bash
# 压缩封面图和配图
baoyu-compress-image \
  --input cover.png \
  --quality 85 \
  --format webp \
  --output cover_compressed.webp
```

#### 10.3 多平台发布（第 2 批）
```bash
# 发布到微信公众号
baoyu-post-to-wechat \
  --title "视频标题" \
  --content video_intro.md \
  --images cover_compressed.webp \
  --video final_video.mp4

# 发布到微博
baoyu-post-to-weibo \
  --text "视频简介 #话题#" \
  --images cover.webp \
  --video final_video.mp4

# 发布到 Twitter/X
baoyu-post-to-x \
  --text "Video description #hashtag" \
  --media final_video.mp4
```

---

## 工作流卡片设计

### video-production-pipeline.card
```yaml
名称：一体化视频生成（23 技能整合版）
触发词：
  - "做个视频"
  - "生成视频"
  - "视频制作"
  - "从零开始做视频"
  - "没有素材做个视频"
  - "下载 YouTube 视频做参考"
  - "多语言视频"

包含子技能（完整）：
  第 0 批（资料）:
    - baoyu-youtube-transcript（YouTube 下载）
    - baoyu-url-to-markdown（网页提取）
  第 1 批（创意）:
    - brainstorming（头脑风暴）
  第 2 批（脚本）:
    - 文案生成（Claude）
  第 3 批（配音）:
    - tts-voice（配音+VTT）
  第 4 批（分镜）:
    - 分镜脚本生成（Claude）
  第 5 批（素材）:
    - baoyu-image-gen（通用图片）
    - baoyu-infographic（数据图表）
    - baoyu-cover-image（封面图）
    - baoyu-diagram（流程图）
    - baoyu-article-illustrator（旁白配图）
    - baoyu-image-cards（信息卡片）
    - baoyu-comic（漫画分镜）
    - baoyu-xhs-images（小红书配图）
  第 6 批（视频）:
    - FFmpeg（图片转视频）
    - hyperframes（HTML 动画）
  第 7 批（字幕）:
    - auto-subtitles（字幕同步）
    - baoyu-translate（多语言翻译）
  第 8 批（BGM）:
    - music-gen（背景音乐）
  第 9 批（剪辑）:
    - video-use（最终合成）
  第 10 批（发布）:
    - baoyu-format-markdown（格式优化）
    - baoyu-compress-image（图片压缩）
    - baoyu-post-to-wechat（公众号）
    - baoyu-post-to-weibo（微博）
    - baoyu-post-to-x（Twitter）

预设模板：
  - 产品展示视频（竖屏 60 秒）
  - 知识科普视频（横屏 180 秒）
  - 口播视频（竖屏 30 秒）
  - YouTube 视频（横屏 10 分钟）
  - 小红书视频（竖屏 90 秒）
```

---

## 自动化脚本

### make_video.sh（总控脚本）
```bash
#!/bin/bash

# 一体化视频生成脚本（23 技能整合版）
# 用法：./make_video.sh <主题> <时长> <风格> <平台>

THEME="$1"        # 如："产品介绍"
DURATION="$2"     # 如：60
STYLE="$3"        # 如："professional"
PLATFORM="$4"     # 如："youtube"

echo "=== 开始视频制作 ==="
echo "主题：$THEME"
echo "时长：${DURATION}秒"
echo "风格：$STYLE"
echo "平台：$PLATFORM"

# 0. 下载参考资料
echo "[0/12] 下载参考资料..."
./download_refs.sh "$THEME"

# 1. 头脑风暴
echo "[1/12] 头脑风暴..."
./brainstorm.sh "$THEME"

# 2. 生成脚本
echo "[2/12] 生成脚本..."
./gen_script.sh "$THEME" "$DURATION"

# 3. 生成配音
echo "[3/12] 生成配音..."
./gen_voice.sh script.txt narration.mp3

# 4. 生成分镜
echo "[4/12] 生成分镜脚本..."
./gen_storyboard.sh script.txt narration.mp3

# 5. 生成素材
echo "[5/12] 生成素材（图片/图表/封面）..."
./gen_assets.sh storyboard.json

# 6. 生成视频序列
echo "[6/12] 图片转视频..."
./images_to_video.sh frame_*.png

# 7. 生成字幕
echo "[7/12] 生成字幕..."
./gen_subtitles.sh narration.mp3

# 8. 翻译多语言字幕
echo "[8/12] 翻译字幕..."
./translate_subs.sh subtitles.srt

# 9. 生成 BGM
echo "[9/12] 生成背景音乐..."
./gen_bgm.sh "$STYLE"

# 10. 最终合成
echo "[10/12] 合成视频..."
./merge_all.sh

# 11. 压缩优化
echo "[11/12] 图片压缩..."
./compress_images.sh

# 12. 多平台发布
echo "[12/12] 发布到 $PLATFORM..."
./publish.sh "$PLATFORM"

echo "✅ 视频制作完成！"
```

---

## 质量检查清单

### 视频质量
- [ ] 分辨率：1080p 或 720p
- [ ] 帧率：30fps
- [ ] 码率：视频>8Mbps，音频>192kbps
- [ ] 格式：MP4（H.264 + AAC）

### 音频质量
- [ ] 配音清晰无杂音
- [ ] BGM 不压过人声（-20dB）
- [ ] 音量标准化（-16LUFS）
- [ ] 无爆音/断点

### 字幕质量
- [ ] 无错别字
- [ ] 时间轴准确（±50ms）
- [ ] 字体大小适中（>24px）
- [ ] 多语言版本完整

### 画面质量
- [ ] 画面与配音同步
- [ ] 转场流畅
- [ ] 色彩协调
- [ ] 图表清晰可读

### 发布质量
- [ ] 封面图压缩优化
- [ ] 多平台格式适配
- [ ] 话题标签正确
- [ ] 描述文案完整

---

## 技能批次汇总

### 第 1 批（视频增强）
- ✅ baoyu-youtube-transcript
- ✅ baoyu-url-to-markdown
- ✅ baoyu-diagram
- ✅ baoyu-translate
- ✅ baoyu-format-markdown

### 第 2 批（发布增强）
- ✅ baoyu-slide-deck
- ✅ baoyu-markdown-to-html
- ✅ baoyu-post-to-wechat/weibo/x
- ✅ baoyu-compress-image

### 第 3 批（通用增强）
- ✅ baoyu-article-illustrator
- ✅ baoyu-image-cards
- ✅ brainstorming
- ✅ caveman-commit

### 整合技能（无需批次）
- ✅ baoyu-imagine（替换 baoyu-image-gen，高质量图片）
- ✅ baoyu-comic（故事性视频分镜）

---

## 相关文件

- **总控技能**：`workflows/video-production-pipeline.card`
- **配音生成**：`skills/tts-voice/SKILL.md`
- **画面生成**：`skills/hyperframes/` + `skills/baoyu-*/SKILL.md`
- **字幕生成**：`skills/auto-subtitles/` + `skills/baoyu-translate/SKILL.md`
- **BGM 生成**：`skills/music-gen/SKILL.md`
- **最终剪辑**：`skills/video-use/SKILL.md`
- **内容发布**：`skills/content-publish-workflow/SKILL.md`

---

## 版本历史

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2026-05-01 | 初始版本（6 技能） |
| v2.0 | 2026-05-01 | 集成图片生成（10 技能） |
| **v3.0** | **2026-05-01** | **23 技能完整整合（3 批次）** |

---

## 性能指标

| 指标 | v1.0 | v2.0 | v3.0 |
|------|------|------|------|
| 技能数 | 6 | 10 | 23 |
| 自动化程度 | 40% | 65% | 90%+ |
| 适用场景 | 简单图文 | 素材视频 | 全平台发布 |
| 多语言支持 | ❌ | ❌ | ✅ |
| 一键发布 | ❌ | ❌ | ✅ |

---
