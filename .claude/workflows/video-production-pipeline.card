# 🎬 一键视频生成（简化版）

**完整流程**: 脚本 → 配音 → 分镜 → AI 图片 → 视频 → BGM → 成品

---

## 🚀 快速开始

### 一行命令生成视频

```bash
cd /workspace/.claude/scripts
./make_video.sh --theme "视频主题" --duration 180
```

### 使用已有脚本

```bash
./make_video.sh --script script.md --output output/
```

---

## 📦 核心技能

| 阶段 | 技能/工具 | 自动触发 |
|------|-----------|---------|
| 脚本 | Claude/Claude | ✅ |
| 配音 | tts-voice | ✅ |
| 分镜 | Claude | ✅ |
| 图片 | ModelScope API | ✅ |
| 视频 | FFmpeg | ✅ |
| BGM | music-gen | 可选 |

---

## 📋 完整文档

详细文档：[scripts/README.md](scripts/README.md)

### 常用选项

```bash
./make_video.sh \
  --theme "主题" \      # 视频主题
  --duration 180 \      # 时长（秒）
  --voice 语音名 \       # TTS 语音
  --output output/      # 输出目录
```

### 查看帮助

```bash
./make_video.sh --help
```

---

## 🎯 使用场景

### 场景 1：从零开始

```bash
./make_video.sh --theme "德国心脏病桌游介绍" --duration 270
```

**执行流程**:
1. AI 生成脚本
2. TTS 生成配音
3. AI 生成分镜
4. ModelScope 生成图片
5. FFmpeg 合成视频

### 场景 2：有脚本素材

```bash
./make_video.sh --script script.md --output output/
```

**执行流程**:
1. 使用已有脚本
2. 生成配音
3. 生成分镜
4. AI 生成图片
5. 合成视频

---

## 💡 ModelScope AI 图片生成

### 独立使用

```bash
# 单张生成
python3 gen_image.py "提示词" output.png

# 批量生成
python3 gen_image.py batch.json
```

### 配置

```python
BASE_URL = "https://api-inference.modelscope.cn/"
API_KEY = "ms-cc494f31-690a-4df6-9bea-a9a0d3311c4e"
MODEL = "Tongyi-MAI/Z-Image-Turbo"
```

详见：[gen_image.py](scripts/gen_image.py)

---

## 🐛 常见问题

**Q: 图片生成失败？**
- 检查 API key
- 使用 `--skip-images` 跳过
- 手动准备图片

**Q: 配音生成失败？**
- 安装 `pip3 install edge-tts`

**Q: 视频合成失败？**
- 检查 FFmpeg 安装
- 确保图片尺寸统一

---

## 📚 相关文档

- **简化文档**: 本文档（快速上手）
- **完整文档**: [scripts/README.md](scripts/README.md)
- **工作流详情**: [workflows/video-production-pipeline.card](workflows/video-production-pipeline.card)
- **问题报告**: [VIDEO_WORKFLOW_ISSUES.md](VIDEO_WORKFLOW_ISSUES.md)

---

**版本**: v1.0 (2026-05-02)  
**维护**: MonkeyCode AI
