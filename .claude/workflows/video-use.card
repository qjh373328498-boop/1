# video-use 工作流 - 有素材时自动剪辑

## 触发关键词
- `剪辑视频` / `剪视频` / `自动剪辑`
- `有原始素材` / `处理视频`
- `去掉废话` / `加字幕` / `调色`
- `精简视频` / `视频切片`

## 置信度判断
- ≥80%: 用户明确提到有视频素材需要处理
- 60-79%: 提到视频但不确定是否有素材
- <60%: 不触发

## 使用场景
当你有原始视频素材（口播、访谈、教程等），需要自动剪辑时使用。

## 工作流程

### 1. 确认素材位置
```bash
# 询问用户视频素材所在的文件夹路径
# 示例：/workspace/videos/raw-footage
```

### 2. 检查环境配置
```bash
# 检查 FFmpeg 是否安装
ffmpeg -version

# 检查 video-use skill 是否已安装
ls ~/.claude/skills/video-use

# 检查 .env 文件是否配置
cat /workspace/.claude/skills/video-use/.env
```

### 3. 初始化项目（首次使用）
```bash
cd <视频素材目录>

# 按照 install.md 初始化
# 会创建 edit/ 输出目录和项目配置文件
```

### 4. 转录素材
```bash
# 调用 ElevenLabs API 转录视频
# 生成 takes_packed.md 文件
```

### 5. 执行剪辑
用户给出指令如：
- "剪辑成 3 分钟的短视频"
- "去掉废话和卡顿"
- "加字幕"

### 6. 输出结果
- 输出位置：`<视频素材目录>/edit/final.mp4`
- 支持自我评估和迭代优化

## 核心优势

1. **文本优先**：通过音频转录理解内容，大幅降低 token 消耗
2. **逐词精度**：精确到毫秒级的剪切点
3. **自动处理**：
   - 删除废话（um, uh 等）
   - 自动调色
   - 添加字幕
   - 30ms 音频渐变
4. **自我评估**：渲染后自动检查质量，最多 3 次迭代

## 依赖要求

- FFmpeg（已安装）
- ElevenLabs API Key（需要用户提供）
- Node.js 环境

## 注意事项

1. 首次使用需要配置 ElevenLabs API Key
2. 转录需要时间（约 1 分钟/10 分钟视频）
3. 输出文件在素材目录的 edit/ 文件夹中
4. 支持自然语言实时修改（"开头太拖拉"、"中间那段删掉"）

## 相关文件
- Skill 目录：`/workspace/.claude/skills/video-use/`
- 使用说明：`SKILL.md`
- 安装指南：`install.md`
