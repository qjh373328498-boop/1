# TTS 语音生成工作流

## 触发关键词
- `转语音` / `生成语音` / `文字转语音`
- `TTS` / `语音合成` / `配音`
- `朗读` / `有声书` / `旁白`
- `男声` / `女声` / `配音员`

## 置信度判断
- ≥80%: 用户明确要求将文字转成语音
- 60-79%: 提到语音但不确定是否需要生成
- <60%: 不触发

## 使用场景
当你需要将文字内容转换成语音文件（MP3）时使用。

## 可用语音列表

### 普通话（推荐）
| 语音名称 | 性别 | 适用场景 | 风格 |
|----------|------|----------|------|
| zh-CN-XiaoxiaoNeural | 女 | 新闻、小说 | 温暖 |
| zh-CN-XiaoyiNeural | 女 | 卡通、小说 | 活泼 |
| zh-CN-YunjianNeural | 男 | 体育、小说 | 激情 |
| zh-CN-YunxiNeural | 男 | 小说 | 阳光 |
| zh-CN-YunxiaNeural | 男 | 卡通、小说 | 可爱 |
| zh-CN-YunyangNeural | 男 | 新闻 | 专业 |

### 方言
| 语音名称 | 地区 | 风格 |
|----------|------|------|
| zh-CN-liaoning-XiaobeiNeural | 东北话 | 幽默 |
| zh-CN-shaanxi-XiaoniNeural | 陕西话 | 明亮 |

### 粤语/台湾
| 语音名称 | 地区 | 风格 |
|----------|------|------|
| zh-HK-HiuGaaiNeural | 粤语 | 友好 |
| zh-HK-HiuMaanNeural | 粤语 | 友好 |
| zh-TW-HsiaoChenNeural | 台湾 | 友好 |

## 工作流程

### 1. 确认需求
- 要转换的文字内容
- 选择语音（默认：zh-CN-YunxiNeural 阳光男声）
- 输出文件名

### 2. 生成语音
```bash
# 基础用法
python3 -m edge_tts --voice zh-CN-YunxiNeural --text "你好，这是测试" --write-media output.mp3

# 从文件读取文字
python3 -m edge_tts --voice zh-CN-XiaoxiaoNeural --file input.txt --write-media output.mp3

# 生成字幕文件
python3 -m edge_tts --voice zh-CN-YunxiNeural --text "你好" --write-media output.mp3 --write-subtitles output.vtt
```

### 3. 输出结果
- 默认格式：MP3
- 输出位置：当前目录或指定路径
- 可选：同时生成字幕文件 (.vtt)

## 核心优势

1. **完全免费**：无需 API key
2. **高质量**：微软 Edge 语音引擎
3. **多语言**：支持全球 100+ 种语言
4. **快速**：实时生成
5. **无需安装额外软件**：Python 环境即可

## 依赖要求

- Python 3.7+
- edge-tts（已安装）
- ffmpeg（可选，用于格式转换）

## 使用示例

### 示例 1：生成旁白
```
用户：帮我把这段文字转成语音，用女声
文字：欢迎来到我们的产品介绍页面...

执行：
python3 -m edge_tts --voice zh-CN-XiaoxiaoNeural \
  --text "欢迎来到我们的产品介绍页面..." \
  --write-media narration.mp3
```

### 示例 2：有声书
```
用户：把这章小说做成有声书，用讲故事的男声

执行：
python3 -m edge_tts --voice zh-CN-YunxiNeural \
  --file chapter1.txt \
  --write-media chapter1.mp3 \
  --write-subtitles chapter1.vtt
```

### 示例 3：多语言配音
```
用户：生成一段英文配音

执行：
python3 -m edge_tts --voice en-US-JennyNeural \
  --text "Welcome to our application" \
  --write-media intro_en.mp3
```

## 常用英语语音

| 语音名称 | 语言 | 风格 |
|----------|------|------|
| en-US-JennyNeural | 英语 (美) | 温暖友好 |
| en-US-GuyNeural | 英语 (美) | 专业清晰 |
| en-GB-SoniaNeural | 英语 (英) | 优雅 |
| en-AU-NatashaNeural | 英语 (澳) | 友善 |

## 注意事项

1. 需要联网才能调用微软服务
2. 长文本建议分段生成（每段<5000 字）
3. 生成速度取决于网络状况
4. 建议保存原始文字稿便于修改

## 相关文件
- 技能目录：`/workspace/.claude/skills/tts-voice/`
- 官方文档：https://github.com/rany2/edge-tts
