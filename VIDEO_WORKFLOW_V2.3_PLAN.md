# 视频生成工作流 V2.3 规划

## V2.3 新功能（规划中）

### 1. 批量任务处理 ✅ 部分实现

**功能描述**: 从 JSON 配置文件加载多个视频任务，自动依次处理。

**使用方法**:
```bash
./make_video.sh --batch tasks.json --output ./batch-output/
```

**配置文件示例** (`tasks.json`):
```json
[
  {"theme": "AI 绘画教程", "duration": 60, "voice": "zh-CN-YunxiNeural"},
  {"theme": "Python 入门", "duration": 90, "voice": "zh-CN-XiaoxiaoNeural"},
  {"theme": "健康饮食", "duration": 120, "voice": "zh-CN-YunjianNeural"}
]
```

**实现状态**: ⏳ 代码已添加，需要测试验证

---

### 2. 智能进度预估 ✅ 部分实现

**功能描述**: 根据已完成步骤的时间，预估剩余时间和完成时间。

**显示内容**:
- ⏱️ 已用时：2 分 30 秒
- 预计剩余：5 分 15 秒
- 预计完成：14:35:20

**实现状态**: ⏳ 函数已添加，需要集成到主流程

---

### 3. 错误恢复机制 ✅ 部分实现

**功能描述**: 任务失败时自动重试或继续执行下一个任务。

**错误处理策略**:
- `stop`: 立即停止（默认）
- `continue`: 跳过失败任务，继续下一个
- `retry`: 自动重试（最多 N 次）

**配置参数**:
```bash
--error-handling retry
--max-retries 5
```

**实现状态**: ⏳ 函数已添加，需要集成

---

## V2.2 已完成功能（当前稳定版）

### ✅ JSON 配置文件
```bash
./make_video.sh --config config.json
```

### ✅ 断点续传
```bash
./make_video.sh --resume-from 3 --output ./existing-output/
```

### ✅ 质量检查
自动生成 `quality_report.txt`，检查：
- 音频时长和内容
- 图片尺寸和完整性
- 视频规格和质量

### ✅ 可配置参数
```bash
--width 1920 --height 1080     # 分辨率
--crf 18                        # 视频质量 (0-51)
--audio-bitrate 256k           # 音频质量
```

---

## 下一步计划

### P1 - 立即可用
1. 测试 V2.2 所有功能
2. 编写使用文档
3. 创建示例配置文件

### P2 - 本周完成
1. 完善批量任务功能
2. 集成智能进度预估
3. 测试错误恢复机制

### P3 - 未来改进
1. 并发任务处理 (--concurrent N)
2. 进度保存/恢复 (checkpoint)
3. Web 界面
4. API 接口

---

## 版本历史

| 版本 | 日期 | 主要功能 |
|------|------|----------|
| V2.3 | 2026-05-02 | 批量任务 + 智能进度 + 错误恢复 (开发中) |
| V2.2 | 2026-05-02 | 配置文件 + 断点续传 + 质量检查 ✅ |
| V2.1 | 2026-05-02 | 进度显示 + 日志记录 + BGM ✅ |
| V2.0 | 2026-05-01 | 智能脚本生成 + 动态分镜 ✅ |
| V1.0 | 2026-04-30 | 基础一键脚本 ✅ |

